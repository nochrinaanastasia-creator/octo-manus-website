[build]
  command   = "npm run build"
  publish   = "dist/client"

# SPA fallback — serve index.html for all routes so TanStack Router
# can handle client-side navigation from any direct URL.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
